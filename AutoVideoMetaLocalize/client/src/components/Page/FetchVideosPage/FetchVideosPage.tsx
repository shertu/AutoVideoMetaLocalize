import * as React from 'react';
import './style.less';

/**
 * A mailto link which provides the email address of the developer.
 *
 * @return {JSX.Element}
 */
export function FetchVideosPage(): JSX.Element {
  return (
    <p>Fetch videos page</p>
  );
}
